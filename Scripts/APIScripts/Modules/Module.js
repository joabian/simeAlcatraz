var app;
(function () {
    //, 'ngFileUpload'
    'use strict';

    app = angular.module("simeAlcatraz", ['datatables', 'ngResource', 'app.photo']);

    // Handle routing errors and success events
    app.run([function () {
    }]);

})();